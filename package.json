{
  "name": "seo-meeting-productivity-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "trending": "npx tsx scripts/fetch-trending.ts",
    "google-trends": "npx tsx scripts/fetch-google-trends.ts",
    "gen-page": "npx tsx scripts/generate-page-template.ts",
    "validate": "npx tsx scripts/validate-pages.ts"
  },
  "dependencies": {
    "astro": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3",
    "tsx": "^4.0.0",
    "vitest": "^3.1.1"
  }
}
